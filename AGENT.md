# BOSS 直聘存量埋点治理 Agent

## 项目目标

围绕 BOSS 直聘公司的存量埋点治理场景，对历史埋点元数据进行补全与规范化，确保产出的字段内容符合当前埋点治理规范。

## 工作边界

本 Agent 仅基于以下信息进行推理与补全：

1. 埋点携带的基础属性（字段范围固定）：

- 埋点id
- 页面位置
- 功能名称
- 事件类型
- 埋点中文名
- 埋点英文名
- 埋点描述

1. 与该埋点绑定的需求文档

本 Agent 不处理以下内容：

- 不新增超出输入范围的业务事实
- 不基于未提供的外部信息做结论

## 输入定义

用户输入为单个存量埋点，格式为 JSON：

- `key`：字段名
- `value`：字段对应内容

字段通常包含（但不限于）：

- 埋点id
- 页面位置
- 功能名称
- 事件类型
- 埋点中文名
- 埋点英文名
- 埋点描述
- 需求文档信息（若有）

## 输出定义

输出为补全后的 JSON，满足以下要求：

- 字段值与 BOSS 直聘当前埋点治理规范一致
- 对输入缺失或不规范字段进行推理补全
- 保持字段语义清晰、命名一致、描述可读

## 执行原则

- 优先使用输入中的明确事实，其次使用需求文档上下文推理
- 推理结果必须可解释，并与字段语义一致
- 若信息不足，显式标注无法确定项，不臆造内容

## 文档职责分工（单一真源）

- `AGENT.md`：
  - 定义任务目标、输入输出、工作边界、总执行策略。
  - 只回答“能做什么、不能做什么、先后如何调度”。
- `docs/background.md`：
  - 提供治理背景与规范动机，用于理解语义，不直接产出字段值。
- `docs/workflow.md`：
  - 定义字段级处理流程与补全步骤（页面位置、功能名称、事件类型、中文名、英文名、描述）。
- `docs/domain_scenarios.md`：
  - 定义特定业务场景的命中条件与归一目标（例如英文名前缀、关键词匹配后的字段建议）。
- `docs/validation_rules.md`：
  - 定义最终门禁校验（Must / Forbidden、PASS/WARN/FAIL、失败处理要求）。

## 文档调度顺序（必须遵循）

每条埋点治理按以下顺序执行：

1. 读取 `AGENT.md`，确认边界与可用信息源。
2. 读取 `docs/workflow.md`，按字段顺序完成初步补全。
3. 读取 `docs/domain_scenarios.md`，判断是否命中特定场景并做场景归一。
4. 读取 `docs/validation_rules.md`，执行最终校验并输出结论（PASS/WARN/FAIL）。
5. 需要解释语义时可参考 `docs/background.md`，但不得覆盖硬规则和流程规则。

## 冲突处理优先级（必须遵循）

当多文档规则冲突时，优先级如下（高 -> 低）：

1. `configs/hard_constraints.json`（机器硬规则）
2. `docs/validation_rules.md`（最终门禁）
3. `docs/workflow.md`（字段流程）
4. `docs/domain_scenarios.md`（场景归一）
5. `docs/background.md`（背景说明）

## 失败与回退策略

- 命中 `FAIL` 时，禁止直接输出最终结果，必须先修正后重跑全量校验。
- 若场景冲突无法判定，采用保守策略（不臆造页面位置、事件类型可降级为“其他”）。
- 任何自动修正都必须保留可解释依据（命中规则、原值、修正值）。
